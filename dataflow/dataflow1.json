{
	"name": "dataflow1",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "ABI_job_list_xlsx",
						"type": "DatasetReference"
					},
					"name": "BoxList"
				},
				{
					"dataset": {
						"referenceName": "Json1",
						"type": "DatasetReference"
					},
					"name": "BoxJobMap"
				},
				{
					"dataset": {
						"referenceName": "table_from_query_txt",
						"type": "DatasetReference"
					},
					"name": "TableFromQuery"
				},
				{
					"dataset": {
						"referenceName": "TableFromView",
						"type": "DatasetReference"
					},
					"name": "TableFromView"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "BoxesTable",
						"type": "DatasetReference"
					},
					"name": "SinkToBoxesTable"
				}
			],
			"transformations": [
				{
					"name": "SelectBoxColumns"
				},
				{
					"name": "JoinBoxAndJob"
				},
				{
					"name": "UpsertBoxes"
				}
			],
			"script": "source(output(\n\t\t{Job Name} as string,\n\t\tStatus as string,\n\t\t{Is TD Extract box} as string,\n\t\t{TD extract job 1} as string,\n\t\t{TD extract job 2} as string,\n\t\t{BQ Dataset} as string,\n\t\t{BQ Tables} as string,\n\t\t{TD Tables} as string,\n\t\tprerequisite_1 as string,\n\t\tprerequisite_2 as string,\n\t\tprerequisite_3 as string,\n\t\tprerequisite_4 as string,\n\t\tprerequisite_5 as string,\n\t\tprerequisite_6 as string,\n\t\tprerequisite_7 as string,\n\t\tprerequisite_8 as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tignoreNoFilesFound: false) ~> BoxList\nsource(output(\n\t\tbox as string,\n\t\tjobs as (name as string, script as string, query as string, table as string[])[]\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tignoreNoFilesFound: false,\n\tdocumentForm: 'documentPerLine') ~> BoxJobMap\nsource(output(\n\t\tFilePath as string,\n\t\tLineNumber as string,\n\t\tTableName as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tignoreNoFilesFound: false) ~> TableFromQuery\nsource(output(\n\t\tFilePath as string,\n\t\tLineNumber as string,\n\t\tViewName as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tignoreNoFilesFound: false) ~> TableFromView\nBoxList select(mapColumn(\n\t\t{Job Name},\n\t\tStatus,\n\t\t{Is TD Extract box}\n\t),\n\tskipDuplicateMapInputs: true,\n\tskipDuplicateMapOutputs: true) ~> SelectBoxColumns\nBoxJobMap, SelectBoxColumns join(box == {Job Name},\n\tjoinType:'inner',\n\tbroadcast: 'auto')~> JoinBoxAndJob\nSelectBoxColumns alterRow() ~> UpsertBoxes\nUpsertBoxes sink(input(\n\t\tID as integer,\n\t\tName as string,\n\t\tHasTDExtraction as boolean,\n\t\tStatus as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tdeletable:false,\n\tinsertable:true,\n\tupdateable:false,\n\tupsertable:false,\n\tformat: 'table',\n\tskipDuplicateMapInputs: true,\n\tskipDuplicateMapOutputs: true,\n\terrorHandlingOption: 'stopOnFirstError') ~> SinkToBoxesTable"
		}
	}
}